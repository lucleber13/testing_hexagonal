/**
 * Slice tests for REST controllers using {@code @WebMvcTest}.
 *
 * <p>Only the web layer is loaded; service dependencies are mocked.
 * Verify HTTP status codes, JSON serialisation, and validation behaviour.
 *
 * <p>Naming convention: {@code <Controller>Test}
 * (e.g. {@code UserControllerTest}).
 */
package com.cbcode.hexagonalspringboottemplate.infrastructure.adapter.input.web;
