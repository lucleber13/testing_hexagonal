/**
 * Slice tests for JPA persistence adapters using {@code @DataJpaTest}.
 *
 * <p>Runs against an in-memory H2 database (test profile). Verifies that
 * adapters correctly translate between JPA entities and domain objects.
 *
 * <p>Naming convention: {@code <Adapter>Test}
 * (e.g. {@code UserPersistenceAdapterTest}).
 */
package com.cbcode.testinghexagonal.infrastructure.adapter.output.persistence;
