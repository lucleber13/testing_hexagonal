/**
 * JPA entity classes — the persistence model.
 *
 * <p>These classes are annotated with {@code @Entity} and map to database
 * tables managed by Flyway. They must never be exposed to the domain layer.
 *
 * <p>Naming convention: {@code <Entity>Entity}
 * (e.g. {@code UserEntity}, {@code OrderEntity}).
 */
package com.cbcode.hexagonalspringboottemplate.infrastructure.adapter.output.persistence.entity;
