/**
 * Domain model layer.
 *
 * <p>Contains pure domain entities and value objects. No framework annotations
 * should appear here — only plain Java that expresses the business concepts.
 *
 * <p>Typical classes:
 * <ul>
 *   <li>Aggregate roots (e.g. {@code User}, {@code Order})</li>
 *   <li>Value objects (e.g. {@code Email}, {@code Money})</li>
 *   <li>Domain events (e.g. {@code UserRegisteredEvent})</li>
 * </ul>
 */
package com.cbcode.hexagonalspringboottemplate.domain.model;
