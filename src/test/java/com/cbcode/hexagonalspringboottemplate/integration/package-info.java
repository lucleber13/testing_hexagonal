/**
 * Full-stack integration tests using {@code @SpringBootTest}.
 *
 * <p>Loads the complete application context and exercises end-to-end flows.
 * Use the {@code test} Spring profile which points to H2 with Flyway disabled.
 *
 * <p>Naming convention: {@code <Feature>IntegrationTest}
 * (e.g. {@code UserRegistrationIntegrationTest}).
 */
package com.cbcode.hexagonalspringboottemplate.integration;
