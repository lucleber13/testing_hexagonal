/**
 * Spring Data JPA repository interfaces.
 *
 * <p>Extend {@code JpaRepository} or {@code CrudRepository} here.
 * These are used exclusively by the persistence adapters in the parent package
 * and must not be injected elsewhere.
 *
 * <p>Naming convention: {@code <Entity>JpaRepository}
 * (e.g. {@code UserJpaRepository}).
 */
package com.cbcode.hexagonalspringboottemplate.infrastructure.adapter.output.persistence.repository;
